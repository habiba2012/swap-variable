var x= 5;
var y = 10;

function swap(x, y) {
  return [y, x]
}

//[x, y] = swap(x, y)

if (x!=y){
	x=Y;
	y=x;
	document.write("value of x = " + x +"/" + "value of y = " + y);
}

else

document.write("x= "+ x + "y = " + y);